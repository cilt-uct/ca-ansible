echo Starting lecturesight
# Wait for the network to come up properly
sleep 5

# Support starting LS as the galicaster user from an ssh session
export DISPLAY=:0

/usr/bin/logger Starting lecturesight wrapper
cd /opt/ls
/usr/bin/sudo /opt/ls/start_lecturesight.sh &

